import { SignUpFormValidators } from './sign-up-form-validators';

describe('SignUpFormValidators', () => {
  it('should create an instance', () => {
    expect(new SignUpFormValidators()).toBeTruthy();
  });
});
